{
  "rules": {
    "games":{
      "current":{
        ".read": "auth != null",
    		".write": "auth != null"
      },
      "finished":{
        ".read": true,
        "$gameId":{
    			".write": "auth != null && (!data.child('winner').exists() || !newData.exists())"
        }

      }

    },
      "teams":{
        ".read": true,
    		".write": "auth != null"
      }
  }
}